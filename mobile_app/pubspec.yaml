name: face_swap_app
description: A face swap app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  http: ^0.13.6
  share_plus: ^7.2.1
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.0

flutter:
  uses-material-design: true